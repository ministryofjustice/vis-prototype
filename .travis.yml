language: ruby
rvm: 2.1.0
node_js: 0.10
before_script:
  - npm install -g bower
  - bower install
  - bundle exec middleman build
script: echo 0
after_success: test $TRAVIS_PULL_REQUEST == "false" && test $TRAVIS_BRANCH == "master" && bundle exec middleman deploy
